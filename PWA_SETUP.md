# 🚀 PWA Setup Guide - Crypto Monitor

## ✅ Что уже настроено

Ваше приложение полностью готово к работе как PWA (Progressive Web App)!

### Установленные пакеты
- ✅ `@serwist/next@^9.3.0` - Next.js интеграция
- ✅ `serwist@^9.3.0` - Service Worker библиотека
- ✅ `@serwist/cli@^9.3.0` - CLI инструменты

### Настроенные файлы
- ✅ `next.config.ts` - конфигурация Serwist
- ✅ `public/sw.ts` - исходный код Service Worker
- ✅ `public/sw.js` - скомпилированный Service Worker
- ✅ `public/manifest.json` - манифест PWA
- ✅ `app/ServiceWorkerRegistration.tsx` - регистрация SW
- ✅ `app/layout.tsx` - метаданные для PWA
- ✅ Иконки: 192x192, 512x512, apple-touch-icon

## 🛠 Как запустить PWA

### 1. Production сборка (обязательно!)

Service Worker работает **только в production режиме**. 

```bash
# Сборка приложения
npm run build

# Запуск production сервера
npm start
```

### 2. Открытие приложения

После запуска откройте приложение в браузере:

```
http://localhost:3000
```

или если у вас настроен HTTPS:

```
https://localhost:3000
```

### 3. Установка PWA

#### На Desktop (Chrome/Edge):
1. Откройте приложение
2. Справа в адресной строке появится иконка установки (⊕)
3. Нажмите на неё и выберите "Установить"
4. Приложение установится как десктопное

#### На Mobile (Android):
1. Откройте приложение в Chrome/Samsung Internet
2. Нажмите меню (⋮) → "Установить приложение" или "Добавить на главный экран"
3. Подтвердите установку
4. Иконка появится на главном экране

#### На iOS:
1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться" (⬆️)
3. Выберите "На экран Домой"
4. Нажмите "Добавить"

## 🔍 Проверка работы Service Worker

### В браузере

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Application**
3. В меню слева выберите **Service Workers**
4. Вы должны увидеть активный Service Worker для вашего сайта

### Проверка в консоли

После загрузки страницы в консоли должно появиться:
```
Service Worker registered with scope: http://localhost:3000/
```

## 📋 Возможности PWA

После установки приложение может:

- ✅ **Работать оффлайн** - кэширование страниц и ресурсов
- ✅ **Быстрая загрузка** - мгновенный старт приложения
- ✅ **Установка на устройство** - как нативное приложение
- ✅ **Полноэкранный режим** - без адресной строки браузера
- ✅ **Работа в фоне** - через Service Worker
- ✅ **Push уведомления** - (требует дополнительной настройки)

## 🐛 Решение проблем

### Service Worker не регистрируется

1. **Проверьте режим**
   - SW работает только в production: `npm run build && npm start`
   - В development режиме SW отключен

2. **Проверьте HTTPS**
   - Service Workers требуют HTTPS (кроме localhost)
   - Используйте HTTPS или localhost для тестирования

3. **Очистите кэш**
   ```
   DevTools → Application → Clear storage → Clear site data
   ```

### Приложение не обновляется

1. **Отмените регистрацию старого SW**
   ```
   DevTools → Application → Service Workers → Unregister
   ```

2. **Hard Reload**
   ```
   Ctrl + Shift + R (Windows/Linux)
   Cmd + Shift + R (Mac)
   ```

### Кнопка установки не появляется

Проверьте, что выполнены все требования:
- ✅ Приложение работает по HTTPS или localhost
- ✅ Есть manifest.json с корректными данными
- ✅ Есть иконки нужных размеров
- ✅ Service Worker успешно зарегистрирован
- ✅ Приложение имеет start_url

## 📱 Тестирование PWA

### Chrome DevTools Lighthouse

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Lighthouse**
3. Выберите категорию **Progressive Web App**
4. Нажмите **Generate report**
5. Проверьте результаты и рекомендации

### Ожидаемые результаты

- ✅ Installable
- ✅ PWA optimized
- ✅ Fast and reliable
- ✅ Works offline

## 🚀 Деплой на Production

### Vercel (рекомендуется)

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Production деплой
vercel --prod
```

### Другие платформы

Убедитесь что:
1. Используется команда `npm run build && npm start`
2. Настроен HTTPS
3. Все файлы из `public/` доступны
4. Service Worker (sw.js) загружается корректно

## 🎨 Настройка PWA под себя

### Изменить название и описание

Отредактируйте `public/manifest.json`:

```json
{
  "name": "Ваше название",
  "short_name": "Короткое",
  "description": "Ваше описание"
}
```

### Изменить цвета

1. **manifest.json**:
```json
{
  "background_color": "#000000",
  "theme_color": "#ff6b4a"
}
```

2. **app/layout.tsx**:
```typescript
export const metadata: Metadata = {
  themeColor: "#ff6b4a",
  // ...
}
```

### Заменить иконки

Замените файлы в `public/`:
- `icon-192x192.svg` (или .png)
- `icon-512x512.svg` (или .png)
- `apple-touch-icon.svg` (или .png)

Обновите manifest.json с правильными расширениями.

## 📚 Дополнительные ресурсы

- [Serwist Documentation](https://serwist.pages.dev/)
- [Web.dev PWA Guide](https://web.dev/progressive-web-apps/)
- [MDN Service Worker API](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)

## ✨ Готово!

Ваше приложение готово к работе как PWA. Просто соберите и запустите в production режиме:

```bash
npm run build
npm start
```

Затем откройте в браузере и установите на устройство! 🎉

